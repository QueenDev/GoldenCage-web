{%extends "GoldenCageBackOfficeBundle::layoutadmin.html.twig"%}
{%block Content%}
<div class="widget widget-table action-table">
            <div class="widget-header"> <i class="icon-th-list"></i>
              <h3>Les Reclamation Clients</h3>
            </div>
            <!-- /widget-header -->
            <div class="widget-content">
              <table class="table table-striped table-bordered">
                <thead>
                  <tr>
                    <th> N° Reclamation </th>
                    
                    <th> Texte Reclamation </th>
                    <th> Date Reclamation</th>
                    <th> Etat Reclamation</th>
                    <th class="td-actions"> </th>
                  </tr>
                </thead>
                <tbody>
                  {%for o in com%}
                    
                        <tr>
                          <td> {{o.getIdreclamation()}} </td>
                          
                          <td> {{o.getTextereclamation}} </td>
                          <td> {{o.getDatereclamation()|date('d/m/Y')}} </td>
                          <td> {{o.getEtat()}} </td>
                          
                          <td class="td-actions"><a href="javascript:;" class="btn btn-small btn-success"><i class="btn-icon-only icon-ok"> </i></a><a href="javascript:;" class="btn btn-danger btn-small"><i class="btn-icon-only icon-remove"> </i></a></td>
                         </tr>
                      
                    {%endfor%}
                </tbody>
              </table>
            </div>
            <!-- /widget-content --> 
          </div>
{%endblock %}